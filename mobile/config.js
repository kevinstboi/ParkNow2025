// Cambia esta URL por la de tu servidor de producción cuando despliegues la app
export const API_URL = __DEV__ 
  ? 'http://10.0.2.2:5000'  // Para el emulador de Android
  : 'https://tu-servidor-de-produccion.com';
